spring:
  application:
    name: OpenReferralsDemo
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/openreferrals}


server:
  port: 8080

logging:
  level:
    uk.codery.demo.openreferrals: DEBUG
    org.springframework.data.mongodb: DEBUG
